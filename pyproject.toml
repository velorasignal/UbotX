[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ubotx"
version = "0.1.0"
description = "UbotX chatbot package"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["ubotx"]
